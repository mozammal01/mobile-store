import { Outlet } from "react-router-dom";
// import bannerImg from '../assets/mobile-store-banner.png'

const Root = () => {
  return (
    <div>
      <Outlet></Outlet>
    </div>
  );
};

export default Root;